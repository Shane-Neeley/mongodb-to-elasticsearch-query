/**
 * Convert a mongodb query into an ES query.
 *
 * @param  {Object} q - the mongodb query
 * @return {Object} the ES query
 */
module.exports = {
    convert: function(q) {
        var es = {}
        return es
    }
};